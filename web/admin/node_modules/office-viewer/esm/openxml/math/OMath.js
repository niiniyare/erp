var OMath = /** @class */ (function () {
    function OMath() {
    }
    OMath.fromXML = function (word, element) {
        var math = new OMath();
        math.element = element;
        return math;
    };
    return OMath;
}());

export { OMath };
