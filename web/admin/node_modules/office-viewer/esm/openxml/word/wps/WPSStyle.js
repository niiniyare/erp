import { __values } from 'tslib';
import { parseChildColor } from '../../../word/parse/parseChildColor.js';

/**
 * 没去不太清楚这个是什么，目前主要用于形状的颜色
 */
var WPSStyle = /** @class */ (function () {
    function WPSStyle() {
    }
    WPSStyle.fromXML = function (word, element) {
        var e_1, _a;
        var wpsStyle = new WPSStyle();
        try {
            for (var _b = __values(element.children), _c = _b.next(); !_c.done; _c = _b.next()) {
                var child = _c.value;
                var tagName = child.tagName;
                switch (tagName) {
                    case 'a:fillRef':
                        wpsStyle.fillColor = parseChildColor(function (c) { return word.getThemeColor(c); }, child);
                        break;
                    case 'a:lnRef':
                        wpsStyle.lineColor = parseChildColor(function (c) { return word.getThemeColor(c); }, child);
                        break;
                    case 'a:fontRef':
                        wpsStyle.fontColor = parseChildColor(function (c) { return word.getThemeColor(c); }, child);
                        break;
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return wpsStyle;
    };
    return WPSStyle;
}());

export { WPSStyle };
