import { parseShape } from '../../word/parse/parseShape.js';

/**
 * 自定义形状
 */
var CustomGeom = /** @class */ (function () {
    function CustomGeom() {
    }
    CustomGeom.fromXML = function (word, element) {
        var geom = new CustomGeom();
        geom.shape = parseShape(element);
        return geom;
    };
    return CustomGeom;
}());

export { CustomGeom };
