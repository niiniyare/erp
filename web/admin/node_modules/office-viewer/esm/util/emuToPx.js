/**
 * 将 emu 转换为 px
 */
function emuToPx(emu) {
    if (emu === undefined) {
        return 0;
    }
    if (typeof emu === 'string') {
        emu = parseFloat(emu);
    }
    return emu / 9525;
}

export { emuToPx };
