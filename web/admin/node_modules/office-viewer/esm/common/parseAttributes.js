import { __values } from 'tslib';
import { autoParse } from './autoParse.js';

/**
 * 解析子节点的属性
 */
function parseChildrenAttributes(node, attributes, fillDefault) {
    var e_1, _a;
    if (fillDefault === void 0) { fillDefault = false; }
    var result = [];
    try {
        for (var _b = __values((node === null || node === void 0 ? void 0 : node.children) || []), _c = _b.next(); !_c.done; _c = _b.next()) {
            var child = _c.value;
            result.push(autoParse(child, attributes, fillDefault));
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return result;
}

export { parseChildrenAttributes };
