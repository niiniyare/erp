import { __values } from 'tslib';
import { parseRange } from './util/Range.js';
import { makeBlankValue } from './util/makeBlankValue.js';

/**
 * 表格渲染需要有值，所以加个空值
 */
function initValueForTable(worksheet) {
    var e_1, _a;
    var tableParts = worksheet.tableParts || [];
    try {
        for (var tableParts_1 = __values(tableParts), tableParts_1_1 = tableParts_1.next(); !tableParts_1_1.done; tableParts_1_1 = tableParts_1.next()) {
            var tablePart = tableParts_1_1.value;
            if (!tablePart.ref) {
                console.warn('表格缺少 ref 字段', tablePart);
                continue;
            }
            var ref = parseRange(tablePart.ref);
            makeBlankValue(worksheet.cellData, ref);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (tableParts_1_1 && !tableParts_1_1.done && (_a = tableParts_1.return)) _a.call(tableParts_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
}

export { initValueForTable };
