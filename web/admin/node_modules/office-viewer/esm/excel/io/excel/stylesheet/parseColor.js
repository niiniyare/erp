import { autoParse } from '../../../../common/autoParse.js';
import { CT_Color_Attributes } from '../../../../openxml/ExcelTypes.js';

/**
 * 这个比较常用就封装了一下
 */
function parseColor(node) {
    return autoParse(node, CT_Color_Attributes);
}

export { parseColor };
