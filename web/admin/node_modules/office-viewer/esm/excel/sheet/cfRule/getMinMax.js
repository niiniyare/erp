import { __values } from 'tslib';
import { lt, gt } from '../../../util/number.js';

function getMinMax(rangeValues) {
    var e_1, _a;
    var min;
    var max;
    try {
        for (var rangeValues_1 = __values(rangeValues), rangeValues_1_1 = rangeValues_1.next(); !rangeValues_1_1.done; rangeValues_1_1 = rangeValues_1.next()) {
            var value = rangeValues_1_1.value;
            var val = value.value;
            if (val === '' || val === undefined) {
                continue;
            }
            var num = void 0;
            if (typeof val === 'boolean') {
                num = val ? 1 : 0;
            }
            if (typeof val === 'string') {
                num = parseFloat(val);
            }
            if (typeof val === 'number') {
                num = val;
            }
            if (val === undefined) {
                continue;
            }
            if (min === undefined || lt(num, min)) {
                min = num;
            }
            if (max === undefined || gt(num, max)) {
                max = num;
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (rangeValues_1_1 && !rangeValues_1_1.done && (_a = rangeValues_1.return)) _a.call(rangeValues_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return {
        min: min,
        max: max
    };
}

export { getMinMax };
