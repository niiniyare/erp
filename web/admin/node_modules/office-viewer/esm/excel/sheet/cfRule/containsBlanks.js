import { applyCfRuleDxf } from './applyCfRuleDxf.js';

/**
 * 空内容
 */
function containsBlanks(sheet, cellInfo, cfRule) {
    if (cellInfo.value.trim() === '') {
        applyCfRuleDxf(cfRule, sheet, cellInfo);
        return true;
    }
    return false;
}

export { containsBlanks };
