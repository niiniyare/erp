import { __values } from 'tslib';
import { parseRange, isCellInRange } from '../io/excel/util/Range.js';
import { applyTableStyle } from './table/applyTableStyle.js';

/**
 * 对单元格应用表格样式
 */
function applyTablePartsStyle(sheet, cellInfo) {
    var e_1, _a;
    var row = cellInfo.row;
    var col = cellInfo.col;
    try {
        for (var _b = __values(sheet.getTableParts()), _c = _b.next(); !_c.done; _c = _b.next()) {
            var tablePart = _c.value;
            if (!tablePart.ref) {
                console.warn('表格缺少 ref 字段', tablePart);
                continue;
            }
            var ref = parseRange(tablePart.ref);
            if (isCellInRange(ref, row, col)) {
                applyTableStyle(cellInfo, tablePart, ref, sheet, row, col);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
}

export { applyTablePartsStyle };
