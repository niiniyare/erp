import { __assign } from 'tslib';

/**
 * 应用单元格样式，xdf 是指 Differential formatting，所以只应用部分样式
 */
function applyDxf(cellInfo, dxf) {
    if (dxf) {
        if (dxf.font) {
            cellInfo.font = __assign(__assign({}, cellInfo.font), dxf.font);
        }
        if (dxf.fill) {
            cellInfo.fill = __assign(__assign({}, cellInfo.fill), dxf.fill);
        }
        if (dxf.border) {
            cellInfo.border = __assign(__assign({}, cellInfo.border), dxf.border);
        }
    }
}

export { applyDxf };
