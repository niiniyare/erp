import { __assign } from 'tslib';

function hasValue(cellData) {
    if (typeof cellData === 'string') {
        return true;
    }
    // 其实就是除了 blank 之外的
    if (typeof cellData === 'object' &&
        (cellData.type === 'date' ||
            cellData.type === 'error' ||
            cellData.type === 'rich' ||
            cellData.type === 'style' ||
            cellData.type === 'formula')) {
        return true;
    }
    return false;
}
/**
 * 更新数据，目前还不支持富文本
 * @param value
 * @param cellData
 * @returns
 */
function updateValue(value, cellData) {
    if (value === void 0) { value = ''; }
    if (!cellData) {
        return value || '';
    }
    if (typeof cellData === 'string') {
        return value;
    }
    if ('type' in cellData && cellData.type === 'blank') {
        if (cellData.s !== undefined) {
            return {
                type: 'style',
                value: value,
                s: cellData.s
            };
        }
        return value;
    }
    if ('value' in cellData) {
        return __assign(__assign({}, cellData), { value: value });
    }
    return cellData;
}

export { hasValue, updateValue };
