import { __values } from 'tslib';

function inDateGroupItems(dateGroupItems, date) {
    var e_1, _a;
    var inGroup = false;
    try {
        for (var dateGroupItems_1 = __values(dateGroupItems), dateGroupItems_1_1 = dateGroupItems_1.next(); !dateGroupItems_1_1.done; dateGroupItems_1_1 = dateGroupItems_1.next()) {
            var item = dateGroupItems_1_1.value;
            var groupDate = item.date, dateTimeGrouping = item.dateTimeGrouping;
            switch (dateTimeGrouping) {
                case 'year':
                    if (date.getFullYear() === groupDate.getFullYear()) {
                        inGroup = true;
                    }
                    break;
                case 'month':
                    if (date.getFullYear() === groupDate.getFullYear() &&
                        date.getMonth() === groupDate.getMonth()) {
                        inGroup = true;
                    }
                    break;
                case 'day':
                    if (date.getFullYear() === groupDate.getFullYear() &&
                        date.getMonth() === groupDate.getMonth() &&
                        date.getDate() === groupDate.getDate()) {
                        inGroup = true;
                    }
                    break;
                case 'hour':
                    if (date.getFullYear() === groupDate.getFullYear() &&
                        date.getMonth() === groupDate.getMonth() &&
                        date.getDate() === groupDate.getDate() &&
                        date.getHours() === groupDate.getHours()) {
                        inGroup = true;
                    }
                    break;
                case 'minute':
                    if (date.getFullYear() === groupDate.getFullYear() &&
                        date.getMonth() === groupDate.getMonth() &&
                        date.getDate() === groupDate.getDate() &&
                        date.getHours() === groupDate.getHours() &&
                        date.getMinutes() === groupDate.getMinutes()) {
                        inGroup = true;
                    }
                    break;
                case 'second':
                    if (date.getFullYear() === groupDate.getFullYear() &&
                        date.getMonth() === groupDate.getMonth() &&
                        date.getDate() === groupDate.getDate() &&
                        date.getHours() === groupDate.getHours() &&
                        date.getMinutes() === groupDate.getMinutes() &&
                        date.getSeconds() === groupDate.getSeconds()) {
                        inGroup = true;
                    }
                    break;
                default:
                    break;
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (dateGroupItems_1_1 && !dateGroupItems_1_1.done && (_a = dateGroupItems_1.return)) _a.call(dateGroupItems_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return inGroup;
}

export { inDateGroupItems };
