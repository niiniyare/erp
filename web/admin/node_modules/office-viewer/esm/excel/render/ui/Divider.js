import { H } from '../../../util/H.js';

var Divider = /** @class */ (function () {
    function Divider(container) {
        H('div', {
            className: 'excel-divider',
            parent: container
        });
    }
    return Divider;
}());

export { Divider };
