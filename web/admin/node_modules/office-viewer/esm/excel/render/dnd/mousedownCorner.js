import { selectAll } from './selectAll.js';

/**
 * 点击到全选
 * @param workbook
 * @param hitTestResult
 * @returns
 */
function mousedownCorner(workbook) {
    return selectAll(workbook.getActiveSheet().getIndex());
}

export { mousedownCorner };
