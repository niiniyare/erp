import { __assign } from 'tslib';
import { cellToMergeCell } from '../cell/cellToMergeCell.js';
import { findInViewRange } from './findInViewRange.js';

/**
 * 查找单元格
 */
function findCell(region, offsetX, offsetY, gridLineHitRange, viewRange, mergeCells) {
    // 节点，也可能返回合并单元格
    var _a = findInViewRange(offsetX, offsetY, gridLineHitRange, viewRange), row = _a.row, col = _a.col, x = _a.x, y = _a.y, width = _a.width, height = _a.height;
    var mergeCell = cellToMergeCell(row, col, mergeCells);
    return __assign(__assign({ type: 'cell', region: region }, mergeCell), { realCol: col, realRow: row, x: x, y: y, width: width, height: height });
}

export { findCell };
