/**
 * 绘制选中区表头的高亮
 */
function drawSelectionHeaderHighlight(workbook, canvas, x, y, width, height) {
    var _a = workbook
        .getActiveSheet()
        .getRowColSize(), rowHeaderWidth = _a.rowHeaderWidth, colHeaderHeight = _a.colHeaderHeight;
    var renderOptions = workbook.renderOptions;
    if (rowHeaderWidth > 0 && colHeaderHeight > 0) {
        // 绘制上方表头的高亮
        canvas.drawAlphaRect(x, 0, width, colHeaderHeight, renderOptions.selectionBackgroundColor, renderOptions.selectionBackgroundOpacity);
        // 绘制左侧表头的高亮
        canvas.drawAlphaRect(0, y, rowHeaderWidth, height, renderOptions.selectionBackgroundColor, renderOptions.selectionBackgroundOpacity);
    }
}

export { drawSelectionHeaderHighlight };
