import { drawSelectionHeaderHighlight } from './drawSelectionHeaderHighlight.js';
import { getRangePosition } from './getRangePosition.js';

/**
 * 绘制选择整行的选框
 * @param workbook
 * @param canvas
 * @param selection
 * @param range
 */
function drawRowSelection(workbook, canvas, selection, range) {
    var _a = getRangePosition(workbook, selection.region, range), x = _a.x, y = _a.y, width = _a.width, height = _a.height;
    console.log('drawRowSelection', x, y, width, height, range);
    var renderOptions = workbook.renderOptions;
    // 绘制选中的单元格
    canvas.drawStrokeRect(x, y, width, height, renderOptions.selectionBorderColor, 2);
    // 绘制选中区表头的高亮
    drawSelectionHeaderHighlight(workbook, canvas, x, y, width, height);
}

export { drawRowSelection };
