var CURSOR_MAP = {
    'drawing': 'default',
    'cell': 'cell',
    'row-header': 'e-resize',
    'row-grid': 'row-resize',
    'col-header': 's-resize',
    'col-grid': 'col-resize',
    'corner': 'cell'
};
/**
 * 根据 hitTest 结果更新鼠标样式
 */
function updateCursor(container, hitTestResult, pointerOnLink) {
    if (hitTestResult && hitTestResult.type in CURSOR_MAP) {
        container.style.cursor = CURSOR_MAP[hitTestResult.type];
        if (pointerOnLink) {
            container.style.cursor = 'pointer';
        }
    }
}

export { updateCursor };
