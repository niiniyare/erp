import { SheetList } from './SheetList.js';
import { StatusBar } from './StatusBar.js';
import { ZoomLevel } from './ZoomLevel.js';

/**
 * 底部 sheet 页签及其它状态栏
 */
var SheetTabBar = /** @class */ (function () {
    function SheetTabBar(container, workbook, renderOptions) {
        if (!renderOptions.showSheetTabBar) {
            return;
        }
        new SheetList(container, workbook, renderOptions);
        new StatusBar(container, workbook, renderOptions);
        new ZoomLevel(container, workbook, renderOptions);
    }
    return SheetTabBar;
}());

export { SheetTabBar };
