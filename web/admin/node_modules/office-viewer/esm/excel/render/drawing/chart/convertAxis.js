/**
 * 构建坐标轴
 */
/**
 * 将 Excel chart 的坐标轴转换为 Echarts 的坐标轴
 */
function convertAxis(categories, catAx, valAx) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    var xAxis = {};
    var yAxis = {};
    if (catAx) {
        var axis = {
            type: 'category'
        };
        if (((_a = catAx.axPos) === null || _a === void 0 ? void 0 : _a.val) === 'b') {
            xAxis = axis;
        }
        if (((_b = catAx.axPos) === null || _b === void 0 ? void 0 : _b.val) === 't') {
            xAxis = axis;
            axis.position = 'top';
        }
        if (((_c = catAx.axPos) === null || _c === void 0 ? void 0 : _c.val) === 'l') {
            yAxis = axis;
        }
        if (((_d = catAx.axPos) === null || _d === void 0 ? void 0 : _d.val) === 'r') {
            yAxis = axis;
            axis.position = 'right';
        }
    }
    if (valAx) {
        var axis = {
            type: 'value'
        };
        if (((_e = valAx.axPos) === null || _e === void 0 ? void 0 : _e.val) === 'b') {
            xAxis = axis;
        }
        if (((_f = valAx.axPos) === null || _f === void 0 ? void 0 : _f.val) === 't') {
            xAxis = axis;
            axis.position = 'top';
        }
        if (((_g = valAx.axPos) === null || _g === void 0 ? void 0 : _g.val) === 'l') {
            yAxis = axis;
        }
        if (((_h = valAx.axPos) === null || _h === void 0 ? void 0 : _h.val) === 'r') {
            yAxis = axis;
            axis.position = 'right';
        }
    }
    return {
        xAxis: xAxis,
        yAxis: yAxis
    };
}

export { convertAxis };
