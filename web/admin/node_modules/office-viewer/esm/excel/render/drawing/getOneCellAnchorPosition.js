import { emuToPx } from '../../../util/emuToPx.js';

function getOneCellAnchorPosition(oneCellAnchor, sheet) {
    var _a, _b, _c, _d, _e;
    var x = 0;
    var y = 0;
    var width = 0;
    var height = 0;
    var from = (_a = oneCellAnchor.from) === null || _a === void 0 ? void 0 : _a[0];
    if (from) {
        var cellRow = ((_b = from.row) === null || _b === void 0 ? void 0 : _b[0]) || 0;
        var cellRowOffset = emuToPx((_c = from.rowOff) === null || _c === void 0 ? void 0 : _c[0]);
        var cellCol = ((_d = from.col) === null || _d === void 0 ? void 0 : _d[0]) || 0;
        var cellColOffset = emuToPx((_e = from.colOff) === null || _e === void 0 ? void 0 : _e[0]);
        var cellPosition = sheet.getCellPosition(cellRow, cellCol);
        return {
            x: cellPosition.x,
            y: cellPosition.y,
            width: cellColOffset,
            height: cellRowOffset
        };
    }
    return {
        x: x,
        y: y,
        width: width,
        height: height
    };
}

export { getOneCellAnchorPosition };
