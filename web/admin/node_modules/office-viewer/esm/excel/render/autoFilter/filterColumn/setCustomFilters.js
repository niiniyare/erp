/**
 * 设置自定义过滤条件
 */
function setCustomFilters(autoFilter, colIndex, customFilters) {
    var _a;
    var filterColumn = (_a = autoFilter.filterColumn) === null || _a === void 0 ? void 0 : _a.find(function (column) { return column.colId === colIndex; });
    if (filterColumn) {
        filterColumn.customFilters = customFilters;
    }
    else {
        autoFilter.filterColumn = autoFilter.filterColumn || [];
        autoFilter.filterColumn.push({
            colId: colIndex,
            customFilters: customFilters
        });
    }
}

export { setCustomFilters };
