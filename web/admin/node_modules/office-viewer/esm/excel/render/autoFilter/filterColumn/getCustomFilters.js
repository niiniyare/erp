function getCustomFilters(autoFilter, colIndex) {
    var _a;
    var filterColumn = (_a = autoFilter.filterColumn) === null || _a === void 0 ? void 0 : _a.find(function (column) { return column.colId === colIndex; });
    if (filterColumn) {
        return filterColumn.customFilters;
    }
    return null;
}

export { getCustomFilters };
