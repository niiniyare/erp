'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var autoParse = require('../../../common/autoParse.js');
var ExcelTypes = require('../../../openxml/ExcelTypes.js');
var xml = require('../../../util/xml.js');

/**
 * workbookView 配置项，后续依赖这个来延迟解析 sheet
 */
function parseWorkbookView(workbookNode) {
    var workbookViewNode = xml.getNodeByTagName(workbookNode, 'workbookView', true);
    return autoParse.autoParse(workbookViewNode, ExcelTypes.CT_BookView_Attributes);
}

exports.parseWorkbookView = parseWorkbookView;
