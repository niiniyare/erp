'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var joinPath = require('../../../../util/joinPath.js');

function getRelationPath(drawingPath, relationships, rId) {
    var relationship = relationships.find(function (relationship) { return relationship.id === rId; });
    if (relationship) {
        return joinPath.joinPath(drawingPath, '..', relationship.target);
    }
    return null;
}

exports.getRelationPath = getRelationPath;
