'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * 制造空值，这个主要是给 containsBlanks 条件格式用，目前的实现下如果没有值就没法渲染
 */
function makeBlankValue(cellData, range) {
    for (var i = range.startRow; i <= range.endRow; i++) {
        var rowData = cellData[i];
        if (!rowData) {
            rowData = [];
            cellData[i] = rowData;
        }
        for (var j = range.startCol; j <= range.endCol; j++) {
            if (rowData[j] === undefined) {
                rowData[j] = {
                    type: 'blank'
                };
            }
        }
    }
}

exports.makeBlankValue = makeBlankValue;
