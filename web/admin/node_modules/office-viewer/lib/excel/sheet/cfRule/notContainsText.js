'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var applyCfRuleDxf = require('./applyCfRuleDxf.js');

/**
 * 不包含文本
 */
function notContainsText(sheet, cellInfo, cfRule) {
    if (!cfRule.text) {
        return false;
    }
    if (!cellInfo.value.includes(cfRule.text)) {
        applyCfRuleDxf.applyCfRuleDxf(cfRule, sheet, cellInfo);
        return true;
    }
    return false;
}

exports.notContainsText = notContainsText;
