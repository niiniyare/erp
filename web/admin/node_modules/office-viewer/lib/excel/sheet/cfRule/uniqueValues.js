'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');
var applyCfRuleDxf = require('./applyCfRuleDxf.js');

/**
 * 对不重复值应用样式
 */
function uniqueValues(sheet, cellInfo, ranges, cfRule) {
    var e_1, _a;
    var rangeCache = sheet.getRangeCache();
    var rangeUniqueValues = rangeCache.get(ranges, 'uniqueValues');
    if (!rangeUniqueValues) {
        rangeUniqueValues = new Set();
        var allValues = new Set();
        var rangeValues = sheet.getCellValueByRanges(ranges);
        try {
            for (var rangeValues_1 = tslib.__values(rangeValues), rangeValues_1_1 = rangeValues_1.next(); !rangeValues_1_1.done; rangeValues_1_1 = rangeValues_1.next()) {
                var value = rangeValues_1_1.value;
                if (value.text === '') {
                    continue;
                }
                if (allValues.has(value.text)) {
                    rangeUniqueValues.delete(value.text);
                }
                else {
                    allValues.add(value.text);
                    rangeUniqueValues.add(value.text);
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (rangeValues_1_1 && !rangeValues_1_1.done && (_a = rangeValues_1.return)) _a.call(rangeValues_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        rangeCache.set(ranges, 'uniqueValues', rangeUniqueValues);
    }
    if (rangeUniqueValues.has(cellInfo.value)) {
        applyCfRuleDxf.applyCfRuleDxf(cfRule, sheet, cellInfo);
        return true;
    }
    return false;
}

exports.uniqueValues = uniqueValues;
