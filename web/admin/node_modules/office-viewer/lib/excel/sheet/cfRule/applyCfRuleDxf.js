'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var applyDxf = require('../applyDxf.js');

/**
 * 应用单元格样式，xdf 是指 Differential formatting，所以只应用部分样式
 */
function applyCfRuleDxf(cfRule, sheet, cellInfo) {
    var dxfId = cfRule.dxfId || 0;
    var dataProvider = sheet.workbook.getDataProvider();
    var dxf = dataProvider.getDxf(dxfId);
    applyDxf.applyDxf(cellInfo, dxf);
}

exports.applyCfRuleDxf = applyCfRuleDxf;
