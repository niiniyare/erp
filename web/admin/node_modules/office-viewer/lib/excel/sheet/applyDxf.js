'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');

/**
 * 应用单元格样式，xdf 是指 Differential formatting，所以只应用部分样式
 */
function applyDxf(cellInfo, dxf) {
    if (dxf) {
        if (dxf.font) {
            cellInfo.font = tslib.__assign(tslib.__assign({}, cellInfo.font), dxf.font);
        }
        if (dxf.fill) {
            cellInfo.fill = tslib.__assign(tslib.__assign({}, cellInfo.fill), dxf.fill);
        }
        if (dxf.border) {
            cellInfo.border = tslib.__assign(tslib.__assign({}, cellInfo.border), dxf.border);
        }
    }
}

exports.applyDxf = applyDxf;
