'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');

/**
 * 将单元格转成合并单元格
 * @param row
 * @param col
 * @param mergeCells
 */
function cellToMergeCell(row, col, mergeCells) {
    var e_1, _a;
    try {
        for (var mergeCells_1 = tslib.__values(mergeCells), mergeCells_1_1 = mergeCells_1.next(); !mergeCells_1_1.done; mergeCells_1_1 = mergeCells_1.next()) {
            var mergeCell = mergeCells_1_1.value;
            var startRow = mergeCell.startRow, endRow = mergeCell.endRow, startCol = mergeCell.startCol, endCol = mergeCell.endCol;
            if (row >= startRow && row <= endRow && col >= startCol && col <= endCol) {
                return tslib.__assign({}, mergeCell);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (mergeCells_1_1 && !mergeCells_1_1.done && (_a = mergeCells_1.return)) _a.call(mergeCells_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return {
        startRow: row,
        startCol: col,
        endRow: row,
        endCol: col
    };
}

exports.cellToMergeCell = cellToMergeCell;
