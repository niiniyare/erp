'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var presetIcons = require('../../io/excel/preset/presetIcons.js');

function drawIconSet(canvas, icon, x, y, width, height) {
    var iconURL = presetIcons.presetIcons[icon];
    if (!iconURL) {
        console.warn('未知的图标', icon);
        return;
    }
    // 避免挡住网格线
    canvas.drawImageWithCache(iconURL, x + 1, y + 1, height - 2, height - 2);
}

exports.drawIconSet = drawIconSet;
