'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getFrozenRange = require('./getFrozenRange.js');

/**
 * 获取左上角冻结区域的访问
 */
function getFrozenTopLeftViewPointRange(xSplit, ySplit, leftShift, topShift, getRowHeight, getColWidth) {
    var _a = getFrozenRange.getFrozenRange(ySplit, topShift, getRowHeight), rows = _a.indexes, startRowOffset = _a.startOffset, rowSizes = _a.sizes, height = _a.length;
    var _b = getFrozenRange.getFrozenRange(xSplit, leftShift, getColWidth), cols = _b.indexes, startColOffset = _b.startOffset, colSizes = _b.sizes, width = _b.length;
    return {
        region: 'top-left-frozen',
        rows: rows,
        rowSizes: rowSizes,
        height: height,
        startRowOffset: startRowOffset,
        cols: cols,
        colSizes: colSizes,
        width: width,
        startColOffset: startColOffset
    };
}

exports.getFrozenTopLeftViewPointRange = getFrozenTopLeftViewPointRange;
