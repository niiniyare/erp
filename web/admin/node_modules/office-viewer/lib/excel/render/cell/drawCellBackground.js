'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getBackgroundColor = require('./getBackgroundColor.js');

function drawCellBackground(ctx, dataProvider, cellInfo, x, y, width, height) {
    var backgroundColor = getBackgroundColor.getBackgroundColor(dataProvider, cellInfo.fill);
    if (backgroundColor !== 'none') {
        ctx.fillStyle = backgroundColor;
        ctx.fillRect(x, y, width, height);
    }
}

exports.drawCellBackground = drawCellBackground;
