'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * 获取冻结区域的数据范围
 */
function getFrozenRange(split, shift, getSize) {
    var indexes = [];
    var startOffset = 0;
    var sizes = [];
    for (var i = 0; i < split; i++) {
        indexes.push(i);
        var size = getSize(i);
        sizes.push({
            size: size,
            offset: startOffset + shift
        });
        startOffset += size;
    }
    return {
        indexes: indexes,
        sizes: sizes,
        startOffset: 0,
        length: startOffset + shift
    };
}

exports.getFrozenRange = getFrozenRange;
