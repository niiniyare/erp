'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var emuToPx = require('../../../util/emuToPx.js');

/**
 * 获取绝对锚点的位置信息
 * @param absoluteAnchor
 */
function getAbsoluteAnchorPosition(absoluteAnchor) {
    var _a, _b;
    var pos = (_a = absoluteAnchor.pos) === null || _a === void 0 ? void 0 : _a[0];
    if (pos) {
        var x = parseFloat(pos.x);
        var y = parseFloat(pos.y);
        var ext = (_b = absoluteAnchor.ext) === null || _b === void 0 ? void 0 : _b[0];
        if (ext && !isNaN(x) && !isNaN(y)) {
            var width = emuToPx.emuToPx(ext.cx);
            var height = emuToPx.emuToPx(ext.cy);
            return {
                x: x,
                y: y,
                width: width,
                height: height
            };
        }
    }
    return {
        x: 0,
        y: 0,
        width: 0,
        height: 0
    };
}

exports.getAbsoluteAnchorPosition = getAbsoluteAnchorPosition;
