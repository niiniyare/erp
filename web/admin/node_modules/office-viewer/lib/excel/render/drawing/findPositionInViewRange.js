'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * 在 viewRange 中找到 row 和 col 的位置
 */
function findPositionInViewRange(currentSheet, row, col, viewRange) {
    var rowIndex = viewRange.rows.indexOf(row);
    var colIndex = viewRange.cols.indexOf(col);
    var rowSize;
    var colSize;
    if (rowIndex !== -1 && colIndex !== -1) {
        rowSize = viewRange.rowSizes[rowIndex];
        colSize = viewRange.colSizes[colIndex];
        return {
            x: colSize.offset,
            y: rowSize.offset,
            width: colSize.size,
            height: rowSize.size
        };
    }
    var cellPosition = currentSheet.getCellPosition(row, col);
    return {
        x: cellPosition.x,
        y: cellPosition.y,
        width: cellPosition.width,
        height: cellPosition.height
    };
}

exports.findPositionInViewRange = findPositionInViewRange;
