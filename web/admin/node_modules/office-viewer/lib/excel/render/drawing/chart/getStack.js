'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function getStack(grouping) {
    var stack;
    var isPercentStacked = false;
    if (grouping) {
        if (grouping.val === 'stacked') {
            stack = 'x';
        }
        if (grouping.val === 'percentStacked') {
            // 这个在 Echarts 里需要自己计算百分比
            stack = 'x';
            isPercentStacked = true;
        }
    }
    return { stack: stack, isPercentStacked: isPercentStacked };
}

exports.getStack = getStack;
