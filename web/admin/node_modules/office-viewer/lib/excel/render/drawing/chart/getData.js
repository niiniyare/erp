'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * 获取数据，目前只从 cache 里获取，后续改成动态获取
 */
function getData(workbook, val) {
    var _a, _b, _c;
    var ref = (_a = val === null || val === void 0 ? void 0 : val.numRef) === null || _a === void 0 ? void 0 : _a.f;
    if (!ref) {
        return [];
    }
    var seriesData = (((_c = (_b = val === null || val === void 0 ? void 0 : val.numRef) === null || _b === void 0 ? void 0 : _b.numCache) === null || _c === void 0 ? void 0 : _c.pt) || []).map(function (pt) {
        return parseFloat(pt.v);
    });
    return seriesData;
}

exports.getData = getData;
