'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * 将 Excel chart 的图例转换为 Echarts 的图例
 */
function convertLegend(categories, chartLegend) {
    var _a;
    var legend = {
        data: categories
    };
    if (chartLegend) {
        var legendPos = (_a = chartLegend.legendPos) === null || _a === void 0 ? void 0 : _a.val;
        if (legendPos === 'b') {
            legend.orient = 'horizontal';
            legend.bottom = 0;
        }
        if (legendPos === 'r') {
            legend.orient = 'vertical';
            legend.right = 0;
            legend.top = 'center';
        }
        if (legendPos === 't') {
            legend.orient = 'horizontal';
            legend.top = 0;
        }
        if (legendPos === 'l') {
            legend.orient = 'vertical';
            legend.left = 0;
            legend.top = 'center';
        }
        if (legendPos === 'tr') {
            legend.orient = 'horizontal';
            legend.top = 0;
            legend.right = 0;
        }
    }
    return legend;
}

exports.convertLegend = convertLegend;
