'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');
var Consts = require('../Consts.js');
var drawAllSelection = require('./drawAllSelection.js');
var drawCellSelection = require('./drawCellSelection.js');
var drawColSelection = require('./drawColSelection.js');
var drawRowSelection = require('./drawRowSelection.js');

/**
 * 绘制选择区域
 * @param workbook
 * @param canvas
 * @param selection
 */
function drawCellRanges(workbook, canvas, selection) {
    var e_1, _a;
    if (!selection) {
        return;
    }
    try {
        for (var _b = tslib.__values(selection.cellRanges), _c = _b.next(); !_c.done; _c = _b.next()) {
            var range = _c.value;
            if (range.endCol === Consts.MAX_COL && range.endRow === Consts.MAX_ROW) {
                drawAllSelection.drawAllSelection(workbook, canvas, selection);
            }
            else if (range.endCol === Consts.MAX_COL) {
                drawRowSelection.drawRowSelection(workbook, canvas, selection, range);
            }
            else if (range.endRow === Consts.MAX_ROW) {
                drawColSelection.drawColSelection(workbook, canvas, selection, range);
            }
            else {
                drawCellSelection.drawCellSelection(workbook, canvas, selection, range);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
}

exports.drawCellRanges = drawCellRanges;
