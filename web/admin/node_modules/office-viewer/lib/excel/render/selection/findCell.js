'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');
var cellToMergeCell = require('../cell/cellToMergeCell.js');
var findInViewRange = require('./findInViewRange.js');

/**
 * 查找单元格
 */
function findCell(region, offsetX, offsetY, gridLineHitRange, viewRange, mergeCells) {
    // 节点，也可能返回合并单元格
    var _a = findInViewRange.findInViewRange(offsetX, offsetY, gridLineHitRange, viewRange), row = _a.row, col = _a.col, x = _a.x, y = _a.y, width = _a.width, height = _a.height;
    var mergeCell = cellToMergeCell.cellToMergeCell(row, col, mergeCells);
    return tslib.__assign(tslib.__assign({ type: 'cell', region: region }, mergeCell), { realCol: col, realRow: row, x: x, y: y, width: width, height: height });
}

exports.findCell = findCell;
