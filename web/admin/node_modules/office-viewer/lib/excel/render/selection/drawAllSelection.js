'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var drawSelectionHeaderHighlight = require('./drawSelectionHeaderHighlight.js');

/**
 * 绘制全选的选区
 */
function drawAllSelection(workbook, canvas, selection) {
    var x = 0;
    var y = 0;
    var _a = workbook.getViewpointSize(), width = _a.width, height = _a.height;
    var renderOptions = workbook.renderOptions;
    // 绘制选中的单元格
    canvas.drawStrokeRect(x, y, width, height, renderOptions.selectionBorderColor, 2);
    // 绘制选中区表头的高亮
    drawSelectionHeaderHighlight.drawSelectionHeaderHighlight(workbook, canvas, x, y, width, height);
}

exports.drawAllSelection = drawAllSelection;
