'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var drawSelectionHeaderHighlight = require('./drawSelectionHeaderHighlight.js');
var getRangePosition = require('./getRangePosition.js');

/**
 * 绘制选择整列的选框
 * @param workbook
 * @param canvas
 * @param selection
 * @param range
 */
function drawColSelection(workbook, canvas, selection, range) {
    var _a = getRangePosition.getRangePosition(workbook, selection.region, range), x = _a.x, y = _a.y, width = _a.width, height = _a.height;
    var renderOptions = workbook.renderOptions;
    // 绘制选中的单元格
    canvas.drawStrokeRect(x, y, width, height, renderOptions.selectionBorderColor, 2);
    // 绘制选中区表头的高亮
    drawSelectionHeaderHighlight.drawSelectionHeaderHighlight(workbook, canvas, x, y, width, height);
}

exports.drawColSelection = drawColSelection;
