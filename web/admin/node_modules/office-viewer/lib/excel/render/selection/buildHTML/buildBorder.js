'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var Consts = require('../../Consts.js');

/**
 * 生成边框样式
 */
function buildBorder(position, border, dataProvider) {
    var color = '';
    if (border.color) {
        color = dataProvider.getColor(border.color, Consts.AUTO_COLOR);
    }
    var borderStyle = 'solid';
    switch (border.style) {
        case 'dashDot':
        case 'dashDotDot':
        case 'dashed':
        case 'mediumDashed':
        case 'mediumDashDot':
        case 'mediumDashDotDot':
            borderStyle = 'dashed';
            break;
        case 'dotted':
            borderStyle = 'dotted';
            break;
        case 'double':
            borderStyle = 'double';
            break;
        case 'none':
            return '';
    }
    if (color && color !== 'none') {
        return "".concat(position, ": 1px ").concat(borderStyle, " ").concat(color, ": ");
    }
    return '';
}

exports.buildBorder = buildBorder;
