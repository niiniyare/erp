'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');

/**
 * 获取配置中的过滤值，目前子支持 val 的情况
 */
function getFilterValues(autoFilter, colIndex) {
    var e_1, _a;
    var _b, _c;
    var filterValues = new Set();
    var filterColumn = (_b = autoFilter.filterColumn) === null || _b === void 0 ? void 0 : _b.find(function (column) { return column.colId === colIndex; });
    if (filterColumn) {
        try {
            for (var _d = tslib.__values(((_c = filterColumn.filters) === null || _c === void 0 ? void 0 : _c.filter) || []), _e = _d.next(); !_e.done; _e = _d.next()) {
                var filter = _e.value;
                if (filter.val) {
                    filterValues.add(filter.val);
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
            }
            finally { if (e_1) throw e_1.error; }
        }
    }
    return filterValues;
}

exports.getFilterValues = getFilterValues;
