'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');
var Range = require('../../../io/excel/util/Range.js');

/**
 * 获取配置中这一列的排序状态
 */
function getColumnSortOrder(colIndex, rangeRef, sortState) {
    var e_1, _a;
    if (!sortState) {
        return 'none';
    }
    try {
        for (var _b = tslib.__values(sortState.sortCondition || []), _c = _b.next(); !_c.done; _c = _b.next()) {
            var condition = _c.value;
            var ref = condition.ref;
            if (!ref) {
                continue;
            }
            var conditionRangeRef = Range.parseRange(ref);
            var startCol = rangeRef.startCol, endCol = rangeRef.endCol;
            if (startCol + colIndex === conditionRangeRef.startCol) {
                return condition.descending ? 'desc' : 'asc';
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return 'none';
}

exports.getColumnSortOrder = getColumnSortOrder;
