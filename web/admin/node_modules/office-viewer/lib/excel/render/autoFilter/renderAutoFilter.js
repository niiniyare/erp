'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');
var renderAutoFilterIcon = require('./renderAutoFilterIcon.js');

var tableId = 0;
function renderAutoFilter(currentSheet, dataContainer) {
    var e_1, _a;
    var autoFilter = currentSheet.getAutoFilter();
    if (autoFilter) {
        // sheet 级别的 autoFilter
        renderAutoFilterIcon.renderAutoFilterIcon(currentSheet, autoFilter, 'sheetAutoFilter', dataContainer);
    }
    // 表格级别的 autoFilter
    var tables = currentSheet.getTables();
    try {
        for (var tables_1 = tslib.__values(tables), tables_1_1 = tables_1.next(); !tables_1_1.done; tables_1_1 = tables_1.next()) {
            var table = tables_1_1.value;
            if (table.autoFilter) {
                renderAutoFilterIcon.renderAutoFilterIcon(currentSheet, table.autoFilter, "table-".concat(tableId++), dataContainer);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (tables_1_1 && !tables_1_1.done && (_a = tables_1.return)) _a.call(tables_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
}

exports.renderAutoFilter = renderAutoFilter;
