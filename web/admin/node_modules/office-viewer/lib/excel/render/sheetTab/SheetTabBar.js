'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var SheetList = require('./SheetList.js');
var StatusBar = require('./StatusBar.js');
var ZoomLevel = require('./ZoomLevel.js');

/**
 * 底部 sheet 页签及其它状态栏
 */
var SheetTabBar = /** @class */ (function () {
    function SheetTabBar(container, workbook, renderOptions) {
        if (!renderOptions.showSheetTabBar) {
            return;
        }
        new SheetList.SheetList(container, workbook, renderOptions);
        new StatusBar.StatusBar(container, workbook, renderOptions);
        new ZoomLevel.ZoomLevel(container, workbook, renderOptions);
    }
    return SheetTabBar;
}());

exports.SheetTabBar = SheetTabBar;
