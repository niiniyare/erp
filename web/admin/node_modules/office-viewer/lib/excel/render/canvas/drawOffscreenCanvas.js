'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * 使用 OffscreenCanvas 渲染后获取内容
 * @param func 执行的调用函数
 * @returns
 */
function drawOffscreenCanvas(ratio, width, height, func) {
    var cacheCanvas = new OffscreenCanvas(width * ratio, height * ratio);
    var cacheCtx = cacheCanvas.getContext('2d');
    cacheCtx.scale(ratio, ratio);
    func(cacheCtx);
    return cacheCanvas;
}

exports.drawOffscreenCanvas = drawOffscreenCanvas;
