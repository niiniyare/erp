'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var removeEventListener = require('./removeEventListener.js');
var handleMousedown = require('./handleMousedown.js');

function handleMouseup() {
    var _a, _b;
    handleMousedown.dragState.isDragging = false;
    removeEventListener.removeEventListener();
    if (handleMousedown.dragState.dragType === 'col-grid') {
        (_a = handleMousedown.dragState.workbook) === null || _a === void 0 ? void 0 : _a.uiEvent.emit('DRAG_COL_GRID_LINE_END', handleMousedown.dragState.col, handleMousedown.dragState.tmpColWidth);
    }
    if (handleMousedown.dragState.dragType === 'row-grid') {
        (_b = handleMousedown.dragState.workbook) === null || _b === void 0 ? void 0 : _b.uiEvent.emit('DRAG_ROW_GRID_LINE_END', handleMousedown.dragState.row, handleMousedown.dragState.tmpRowHeight);
    }
    handleMousedown.resetDragState();
}

exports.handleMouseup = handleMouseup;
