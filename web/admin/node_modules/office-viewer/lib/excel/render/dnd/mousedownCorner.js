'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var selectAll = require('./selectAll.js');

/**
 * 点击到全选
 * @param workbook
 * @param hitTestResult
 * @returns
 */
function mousedownCorner(workbook) {
    return selectAll.selectAll(workbook.getActiveSheet().getIndex());
}

exports.mousedownCorner = mousedownCorner;
