'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');
var Consts = require('../Consts.js');

/**
 * 点击到行表头
 * @param workbook
 * @param hitTestResult
 * @returns
 */
function mousedownRowHeader(workbook, hitTestResult) {
    var activeCell = {
        startRow: hitTestResult.startRow,
        startCol: 0,
        endRow: hitTestResult.endRow,
        endCol: 0
    };
    return {
        user: '',
        region: hitTestResult.region,
        selectType: hitTestResult.type,
        activeCell: activeCell,
        sheetIndex: workbook.getActiveSheet().getIndex(),
        cellRanges: [
            tslib.__assign(tslib.__assign({}, activeCell), { endCol: Consts.MAX_COL })
        ]
    };
}

exports.mousedownRowHeader = mousedownRowHeader;
