'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib = require('tslib');
var Consts = require('../Consts.js');

/**
 * 点击到列表头
 * @param workbook
 * @param hitTestResult
 * @returns
 */
function mousedownColHeader(workbook, hitTestResult) {
    var activeCell = {
        startRow: 0,
        startCol: hitTestResult.startCol,
        endRow: 0,
        endCol: hitTestResult.endCol
    };
    return {
        user: '',
        region: hitTestResult.region,
        selectType: hitTestResult.type,
        activeCell: activeCell,
        sheetIndex: workbook.getActiveSheet().getIndex(),
        cellRanges: [
            tslib.__assign(tslib.__assign({}, activeCell), { endRow: Consts.MAX_ROW })
        ]
    };
}

exports.mousedownColHeader = mousedownColHeader;
