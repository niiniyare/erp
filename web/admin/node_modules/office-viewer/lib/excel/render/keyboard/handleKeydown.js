'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var handleCopy = require('./handleCopy.js');
var handleSelectAll = require('./handleSelectAll.js');
var handleSheetArrowKey = require('./handleSheetArrowKey.js');

/**
 * 处理键盘事件
 */
function handleKeydown(e, workbook) {
    handleSelectAll.handleSelectAll(e, workbook);
    handleCopy.handleCopy(e, workbook);
    handleSheetArrowKey.handleSheetCanvasKeydown(e, workbook);
}

exports.handleKeydown = handleKeydown;
