'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * 老的 vml 格式，目前只支持图片
 */
function renderPict(word, pict) {
    if (pict.src) {
        var img = document.createElement('img');
        img.style.position = 'relative';
        img.src = pict.src;
        return img;
    }
    return null;
}

exports.renderPict = renderPict;
