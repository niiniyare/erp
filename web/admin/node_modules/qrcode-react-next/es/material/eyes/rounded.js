import { createRoundedSquareBorderPath as i, createCirclePath as u } from "../../utils/svgPath.js";
function y(n) {
  const { size: e, dotSize: r, borderSize: a, x: t, y: o } = n, d = i(
    t,
    o,
    e,
    r * a,
    r * 2
  ), c = u(
    t + e / 2,
    o + e / 2,
    e / 2 - r * 2
  );
  return {
    eyeBorder: d,
    eyeInner: c
  };
}
export {
  y as default
};
