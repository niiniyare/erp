import { EyeGeneratorProps } from '../../types';
declare function generateCircleEyePath(options: EyeGeneratorProps): {
    eyeBorder: string;
    eyeInner: string;
};
export default generateCircleEyePath;
