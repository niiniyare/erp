import p from "./default.js";
import l from "./rounded.js";
import z from "./circle.js";
import { getEyeSizeAndPositions as g } from "../../utils/helper.js";
const S = {
  default: p,
  rounded: l,
  circle: z
};
function E(e) {
  const r = {
    default: 1,
    sm: 0.8,
    xs: 0.5
  };
  return (s) => {
    const { size: a, dotSize: o, borderSize: d, margin: y } = s, { eyeSize: c, positions: f } = g(a, o, y);
    let t = "", n = "";
    for (const i of f) {
      const { eyeBorder: u, eyeInner: m } = e({
        size: c,
        dotSize: o,
        borderSize: r[d],
        x: i.x,
        y: i.y
      });
      t += u, n += m;
    }
    return {
      eyeBorder: t,
      eyeInner: n
    };
  };
}
function B(e) {
  const r = S[e];
  return E(r);
}
export {
  B as default,
  S as eyeMap
};
