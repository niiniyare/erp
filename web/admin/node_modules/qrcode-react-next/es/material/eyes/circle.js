import { createRingPath as s, createCirclePath as d } from "../../utils/svgPath.js";
function f(o) {
  const { size: e, dotSize: r, borderSize: a, x: t, y: n } = o, c = s(
    t + e / 2,
    n + e / 2,
    e / 2,
    r * a
  ), i = d(
    t + e / 2,
    n + e / 2,
    e / 2 - r * 2
  );
  return {
    eyeBorder: c,
    eyeInner: i
  };
}
export {
  f as default
};
