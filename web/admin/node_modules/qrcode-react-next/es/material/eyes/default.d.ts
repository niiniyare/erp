import { EyeGeneratorProps } from '../../types';
declare function generateDefaultEyePath(options: EyeGeneratorProps): {
    eyeBorder: string;
    eyeInner: string;
};
export default generateDefaultEyePath;
