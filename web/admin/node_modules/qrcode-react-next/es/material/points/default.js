import { createSquarePath as u } from "../../utils/svgPath.js";
function f(r) {
  const { x: n, y: i, dotSize: t, dotScale: c, margin: a } = r;
  let o = "";
  const e = t * c;
  return o += u(
    n * t + (t - e) / 2 + a,
    i * t + (t - e) / 2 + a,
    e
  ), o;
}
export {
  f as default
};
