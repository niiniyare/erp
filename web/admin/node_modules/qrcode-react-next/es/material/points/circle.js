import { createCirclePath as f } from "../../utils/svgPath.js";
function m(c) {
  const { x: l, y: o, dotSize: t, dotScale: i, margin: r } = c;
  let a = "", e = t / 2 * i, n = l * t + (t - e * 2) / 2 + e + r, d = o * t + (t - e * 2) / 2 + e + r;
  return a += f(n, d, e), a;
}
export {
  m as default
};
