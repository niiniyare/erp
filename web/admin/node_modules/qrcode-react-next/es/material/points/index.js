import m from "./default.js";
import p from "./circle.js";
import { isQrCodeEye as d } from "../../utils/helper.js";
const g = {
  default: m,
  circle: p
};
function h(r) {
  const n = {
    default: 1,
    sm: 0.8,
    xs: 0.5
  };
  return (a) => {
    const { modules: o, dotSize: s, pointSize: l, customSize: f, margin: c } = a;
    let i = "";
    for (let t = 0; t < o.length; t++)
      for (let e = 0; e < o.length; e++)
        if (o[t][e] && !d(o, e, t)) {
          let u = f[t][e] || 1;
          i += r({
            x: e,
            y: t,
            dotSize: s,
            dotScale: n[l] * u,
            margin: c
          });
        }
    return i;
  };
}
function x(r) {
  const n = g[r];
  return h(n);
}
export {
  x as default,
  g as pointsMap
};
