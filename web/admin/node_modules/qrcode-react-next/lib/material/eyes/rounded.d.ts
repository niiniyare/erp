import { EyeGeneratorProps } from '../../types';
declare function generateRoundedEyePath(options: EyeGeneratorProps): {
    eyeBorder: string;
    eyeInner: string;
};
export default generateRoundedEyePath;
