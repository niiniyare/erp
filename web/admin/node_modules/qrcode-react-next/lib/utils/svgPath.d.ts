export declare function createCirclePath(x: number, y: number, r: number): string;
/**
 * 绘制正方形路径
 * @param x 起始点x坐标
 * @param y 起始点y坐标
 * @param size 边长
 * @returns d属性值
 */
export declare function createSquarePath(x: number, y: number, size: number): string;
/**
 * 绘制圆角正方形路径
 * @param x 起始点x坐标
 * @param y 起始点y坐标
 * @param size 边长
 * @param radius 圆角半径
 * @returns d属性值
 */
export declare function createRoundedSquarePath(x: number, y: number, size: number, radius: number): string;
/**
 * 绘制正方形边框路径
 * @param x 起始点x坐标
 * @param y 起始点y坐标
 * @param size 边长
 * @param borderWidth 边框宽度
 * @returns d属性值
 */
export declare function createSquareBorderPath(x: number, y: number, size: number, borderWidth: number): string;
/**
 * 绘制圆角正方形路径
 * @param x 起始点x坐标
 * @param y 起始点y坐标
 * @param size 边长
 * @param borderWidth 边框宽度
 * @param radius 圆角半径
 * @returns d属性值
 */
export declare function createRoundedSquareBorderPath(x: number, y: number, size: number, borderWidth: number, radius: number): string;
/**
 * 绘制圆环路径
 * @param x 圆心x坐标
 * @param y 圆心y坐标
 * @param r 半径
 * @param w 圆环宽度
 * @returns d属性值
 */
export declare function createRingPath(x: number, y: number, r: number, w: number): string;
