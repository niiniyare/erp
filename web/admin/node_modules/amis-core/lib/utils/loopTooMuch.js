/**
 * amis-core v6.13.0
 * Copyright 2018-2025 fex
 */

'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var loopCount = {};
var loopTimer = {};
var loopResult = {};
function loopTooMuch(key, maxSpeed) {
    if (maxSpeed === void 0) { maxSpeed = 20; }
    if (loopResult[key]) {
        return loopResult[key];
    }
    clearTimeout(loopTimer[key]);
    loopTimer[key] = setTimeout(function () {
        delete loopCount[key];
        delete loopTimer[key];
    }, 1000);
    if (loopCount[key] && loopCount[key] > maxSpeed) {
        loopResult[key] = true;
        return true;
    }
    loopCount[key] = (loopCount[key] || 0) + 1;
    return false;
}

exports.loopTooMuch = loopTooMuch;
