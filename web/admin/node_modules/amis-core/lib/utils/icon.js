/**
 * amis-core v6.13.0
 * Copyright 2018-2025 fex
 */

'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var isObject = require('lodash/isObject');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var React__default = /*#__PURE__*/_interopDefaultLegacy(React);
var isObject__default = /*#__PURE__*/_interopDefaultLegacy(isObject);

var __react_jsx__ = require('react');
var _J$X_ = (__react_jsx__["default"] || __react_jsx__).createElement;
(__react_jsx__["default"] || __react_jsx__).Fragment;
/**
 * 废弃，不建议使用
 * 判断字符串来生成 i 或 img
 * @param icon icon 设置
 * @param className 内部用的 className
 * @param classNameProp amis 配置里设置的 className
 */
var generateIcon = function (cx, icon, className, classNameProp) {
    if (isObject__default["default"](icon) &&
        icon.type === 'icon' &&
        icon.icon) {
        icon = icon.icon;
    }
    if (React__default["default"].isValidElement(icon)) {
        return icon;
    }
    if (typeof icon !== 'string') {
        if (isObject__default["default"](icon) &&
            typeof icon.id === 'string' &&
            icon.id.startsWith('svg-')) {
            return (_J$X_("svg", { className: cx('icon', 'icon-object', className, classNameProp) },
                _J$X_("use", { xlinkHref: "#".concat(icon.id.replace(/^svg-/, '')) })));
        }
        return;
    }
    var isURLIcon = (icon === null || icon === void 0 ? void 0 : icon.indexOf('.')) !== -1;
    return icon ? (isURLIcon ? (_J$X_("img", { className: cx(className, classNameProp), src: icon, key: icon })) : (_J$X_("i", { className: cx(className, icon, classNameProp), key: icon }))) : null;
};
var customVendor = new Map();
function registerCustomVendor(vendor, fn) {
    customVendor.set(vendor, fn);
}
function getCustomVendor(vendor) {
    if (!vendor) {
        return undefined;
    }
    return customVendor.get(vendor);
}

exports.generateIcon = generateIcon;
exports.getCustomVendor = getCustomVendor;
exports.registerCustomVendor = registerCustomVendor;
