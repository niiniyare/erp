# Build and Run

Build and start the ERP server with all necessary code generation.

```bash
make ci && make run
```

This command runs all essential checks (fmt, lint, test, proto, sqlc) then starts the server.