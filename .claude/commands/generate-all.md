# Generate All Code

Generate all code including Goa, protobuf, SQLC, and mocks.

```bash
make goa && make proto && make sqlc && make mock
```

This command regenerates all auto-generated code for the ERP system.