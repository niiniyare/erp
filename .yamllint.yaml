extends: default

rules:
  line-length:
    max: 120  # Allow longer lines for URLs and examples
  document-start:
    present: false  # Don't require --- at start
  indentation:
    spaces: 2

ignore: |
  gen/
  *.json
  /gen/
  node_modules/
  vendor/
  .git/
