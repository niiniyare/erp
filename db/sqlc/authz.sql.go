// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: authz.sql

package db

import (
	"context"
	"database/sql"

	"github.com/google/uuid"
)

const deactivateRoleAssignment = `-- name: DeactivateRoleAssignment :exec
UPDATE role_assignments
SET    is_active = FALSE
WHERE  subject = $1 AND role_name = $2 AND domain = $3
`

type DeactivateRoleAssignmentParams struct {
	Subject  string `json:"subject"`
	RoleName string `json:"role_name"`
	Domain   string `json:"domain"`
}

func (q *Queries) DeactivateRoleAssignment(ctx context.Context, arg DeactivateRoleAssignmentParams) error {
	_, err := q.db.Exec(ctx, deactivateRoleAssignment, arg.Subject, arg.RoleName, arg.Domain)
	return err
}

const listExpiredActiveRoleNames = `-- name: ListExpiredActiveRoleNames :many
SELECT role_name
FROM   role_assignments
WHERE  subject    = $1
  AND  domain     = $2
  AND  is_active  = TRUE
  AND  expires_at IS NOT NULL
  AND  expires_at < NOW()
`

type ListExpiredActiveRoleNamesParams struct {
	Subject string `json:"subject"`
	Domain  string `json:"domain"`
}

func (q *Queries) ListExpiredActiveRoleNames(ctx context.Context, arg ListExpiredActiveRoleNamesParams) ([]string, error) {
	rows, err := q.db.Query(ctx, listExpiredActiveRoleNames, arg.Subject, arg.Domain)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var role_name string
		if err := rows.Scan(&role_name); err != nil {
			return nil, err
		}
		items = append(items, role_name)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRoleAssignments = `-- name: ListRoleAssignments :many
SELECT
    id, tenant_id, subject, role_name, domain,
    assigned_by, delegated_by, expires_at, is_active, created_at
FROM   role_assignments
WHERE  subject = $1 AND domain = $2
ORDER  BY created_at DESC
`

type ListRoleAssignmentsParams struct {
	Subject string `json:"subject"`
	Domain  string `json:"domain"`
}

func (q *Queries) ListRoleAssignments(ctx context.Context, arg ListRoleAssignmentsParams) ([]*RoleAssignment, error) {
	rows, err := q.db.Query(ctx, listRoleAssignments, arg.Subject, arg.Domain)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*RoleAssignment{}
	for rows.Next() {
		var i RoleAssignment
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Subject,
			&i.RoleName,
			&i.Domain,
			&i.AssignedBy,
			&i.DelegatedBy,
			&i.ExpiresAt,
			&i.IsActive,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertRoleAssignment = `-- name: UpsertRoleAssignment :exec
INSERT INTO role_assignments
    (id, tenant_id, subject, role_name, domain, assigned_by, delegated_by, expires_at, is_active)
VALUES
    ($1, $2, $3, $4, $5, $6, $7, $8, TRUE)
ON CONFLICT (subject, role_name, domain)
DO UPDATE SET
    is_active    = TRUE,
    assigned_by  = EXCLUDED.assigned_by,
    delegated_by = EXCLUDED.delegated_by,
    expires_at   = EXCLUDED.expires_at
`

type UpsertRoleAssignmentParams struct {
	ID          uuid.UUID    `json:"id"`
	TenantID    uuid.UUID    `json:"tenant_id"`
	Subject     string       `json:"subject"`
	RoleName    string       `json:"role_name"`
	Domain      string       `json:"domain"`
	AssignedBy  *string      `json:"assigned_by"`
	DelegatedBy *string      `json:"delegated_by"`
	ExpiresAt   sql.NullTime `json:"expires_at"`
}

func (q *Queries) UpsertRoleAssignment(ctx context.Context, arg UpsertRoleAssignmentParams) error {
	_, err := q.db.Exec(ctx, upsertRoleAssignment,
		arg.ID,
		arg.TenantID,
		arg.Subject,
		arg.RoleName,
		arg.Domain,
		arg.AssignedBy,
		arg.DelegatedBy,
		arg.ExpiresAt,
	)
	return err
}
