-- Extensions are already enabled by migration 000051_tenant_extensions_and_roles.up.sql
-- (pgcrypto, pg_trgm). This file is kept for reference only.
CREATE EXTENSION IF NOT EXISTS pgcrypto;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
