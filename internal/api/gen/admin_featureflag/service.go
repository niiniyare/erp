// Code generated by goa v3.22.1, DO NOT EDIT.
//
// admin-featureflag service
//
// Command:
// $ goa gen github.com/niiniyare/erp/internal/api/design -o internal/api

package adminfeatureflag

import (
	"context"

	goa "goa.design/goa/v3/pkg"
	"goa.design/goa/v3/security"
)

// Administrative interface for feature flag management
type Service interface {
	// Enable multiple feature flags in bulk
	BulkEnable(context.Context, *BulkEnablePayload) (res *BulkEnableResult, err error)
	// Disable multiple feature flags in bulk
	BulkDisable(context.Context, *BulkDisablePayload) (res *BulkDisableResult, err error)
	// Get system health status
	SystemHealth(context.Context, *SystemHealthPayload) (res *SystemHealthResult, err error)
}

// Auther defines the authorization functions to be implemented by the service.
type Auther interface {
	// JWTAuth implements the authorization logic for the JWT security scheme.
	JWTAuth(ctx context.Context, token string, schema *security.JWTScheme) (context.Context, error)
}

// APIName is the name of the API as defined in the design.
const APIName = "Awo"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "1.0.0"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "admin-featureflag"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [3]string{"bulk_enable", "bulk_disable", "system_health"}

// BulkDisablePayload is the payload type of the admin-featureflag service
// bulk_disable method.
type BulkDisablePayload struct {
	// List of flag names to disable
	FlagNames []string
	// Reason for bulk operation
	Reason string
	// Tenant ID
	TenantID string
	// JWT token for authentication
	Token string
}

// BulkDisableResult is the result type of the admin-featureflag service
// bulk_disable method.
type BulkDisableResult struct {
	// Total number of flags requested
	TotalRequested int
	// Number of successfully processed flags
	Successful int
	// Number of failed operations
	Failed int
	// Execution timestamp
	ExecutedAt string
}

// BulkEnablePayload is the payload type of the admin-featureflag service
// bulk_enable method.
type BulkEnablePayload struct {
	// List of flag names to enable
	FlagNames []string
	// Reason for bulk operation
	Reason string
	// Tenant ID
	TenantID string
	// JWT token for authentication
	Token string
}

// BulkEnableResult is the result type of the admin-featureflag service
// bulk_enable method.
type BulkEnableResult struct {
	// Total number of flags requested
	TotalRequested int
	// Number of successfully processed flags
	Successful int
	// Number of failed operations
	Failed int
	// Execution timestamp
	ExecutedAt string
}

// SystemHealthPayload is the payload type of the admin-featureflag service
// system_health method.
type SystemHealthPayload struct {
	// Tenant ID
	TenantID string
	// JWT token for authentication
	Token string
}

// SystemHealthResult is the result type of the admin-featureflag service
// system_health method.
type SystemHealthResult struct {
	// Overall system status
	Status string
	// Health check timestamp
	Timestamp string
	// Database status
	DatabaseStatus string
	// Cache status
	CacheStatus string
	// Overall health score (0-100)
	OverallScore int
}

// MakeBadRequest builds a goa.ServiceError from an error.
func MakeBadRequest(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "bad_request", false, false, false)
}

// MakeUnauthorized builds a goa.ServiceError from an error.
func MakeUnauthorized(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "unauthorized", false, false, false)
}

// MakeInternalError builds a goa.ServiceError from an error.
func MakeInternalError(err error) *goa.ServiceError {
	return goa.NewServiceError(err, "internal_error", false, false, false)
}
