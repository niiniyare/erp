// Code generated by goa v3.21.1, DO NOT EDIT.
//
// erp-server HTTP client CLI support package
//
// Command:
// $ goa gen github.com/niiniyare/erp/internal/api/design -o internal/api

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	tenantc "github.com/niiniyare/erp/internal/api/gen/http/tenant/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `tenant (create|get|list)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` tenant create --body '{
      "name": "Voluptas odit impedit nam cumque ipsam.",
      "plan_type": "Voluptatem facilis reiciendis.",
      "subdomain": "Sunt quos aut."
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		tenantFlags = flag.NewFlagSet("tenant", flag.ContinueOnError)

		tenantCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		tenantCreateBodyFlag = tenantCreateFlags.String("body", "REQUIRED", "")

		tenantGetFlags     = flag.NewFlagSet("get", flag.ExitOnError)
		tenantGetIDFlag    = tenantGetFlags.String("id", "REQUIRED", "Tenant ID")
		tenantGetTokenFlag = tenantGetFlags.String("token", "", "")

		tenantListFlags      = flag.NewFlagSet("list", flag.ExitOnError)
		tenantListOffsetFlag = tenantListFlags.String("offset", "", "")
		tenantListLimitFlag  = tenantListFlags.String("limit", "", "")
		tenantListTokenFlag  = tenantListFlags.String("token", "", "")
	)
	tenantFlags.Usage = tenantUsage
	tenantCreateFlags.Usage = tenantCreateUsage
	tenantGetFlags.Usage = tenantGetUsage
	tenantListFlags.Usage = tenantListUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "tenant":
			svcf = tenantFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "tenant":
			switch epn {
			case "create":
				epf = tenantCreateFlags

			case "get":
				epf = tenantGetFlags

			case "list":
				epf = tenantListFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "tenant":
			c := tenantc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "create":
				endpoint = c.Create()
				data, err = tenantc.BuildCreatePayload(*tenantCreateBodyFlag)
			case "get":
				endpoint = c.Get()
				data, err = tenantc.BuildGetPayload(*tenantGetIDFlag, *tenantGetTokenFlag)
			case "list":
				endpoint = c.List()
				data, err = tenantc.BuildListPayload(*tenantListOffsetFlag, *tenantListLimitFlag, *tenantListTokenFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// tenantUsage displays the usage of the tenant command and its subcommands.
func tenantUsage() {
	fmt.Fprintf(os.Stderr, `Tenant management service
Usage:
    %[1]s [globalflags] tenant COMMAND [flags]

COMMAND:
    create: Create a new tenant
    get: Get tenant by ID
    list: List tenants with pagination

Additional help:
    %[1]s tenant COMMAND --help
`, os.Args[0])
}
func tenantCreateUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] tenant create -body JSON

Create a new tenant
    -body JSON: 

Example:
    %[1]s tenant create --body '{
      "name": "Voluptas odit impedit nam cumque ipsam.",
      "plan_type": "Voluptatem facilis reiciendis.",
      "subdomain": "Sunt quos aut."
   }'
`, os.Args[0])
}

func tenantGetUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] tenant get -id STRING -token STRING

Get tenant by ID
    -id STRING: Tenant ID
    -token STRING: 

Example:
    %[1]s tenant get --id "Vel repellat hic enim praesentium laboriosam." --token "Quo ad consequuntur laboriosam et excepturi."
`, os.Args[0])
}

func tenantListUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] tenant list -offset INT -limit INT -token STRING

List tenants with pagination
    -offset INT: 
    -limit INT: 
    -token STRING: 

Example:
    %[1]s tenant list --offset 1007291501354984921 --limit 6142854046274343050 --token "Qui est voluptatibus quam illo voluptatem."
`, os.Args[0])
}
