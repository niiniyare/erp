// Package domain is reserved for shared value types.
//
// The session types (ResolvedSession, Configuration, EntityScope) live in
// awo.so/internal/core/iam/domain — import from there.
package domain
